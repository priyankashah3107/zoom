import React from "react";
import VideoFooter from "../components/ui/VideoFooter";

export default {
  title: "Components/ui/VideoFooter",
  component: VideoFooter,
};

const Template = (args) => <VideoFooter {...args} />;

export const VidFooter = Template.bind({});
VidFooter.args = {};
