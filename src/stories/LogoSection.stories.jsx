import React from "react";
import LogoSection from "../components/ui/LogoSection";

export default {
  title: "LogoSection",
  component: LogoSection,
};

const Template = (args) => <LogoSection {...args} />;
export const LogoSectionPage = Template.bind({});

LogoSectionPage.args = {};
